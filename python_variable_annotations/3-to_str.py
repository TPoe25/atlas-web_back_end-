#!/usr/bin/env python3
"""
Module that contains a type-annotated func to convert float to str rep
"""


def to_str(n: float) -> str:
    """
    Converts a float to its string representation.

    Args:
        n (float): Number to convert to string.

    Returns:
        str: String representation of the number.
    """
    return str(n)
